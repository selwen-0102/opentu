# Tasks: 画布联想引用与结果连线

## 1. Prompt And Preference

- [x] 1.1 增加默认关闭、存储异常安全回退的联想持久开关及定向测试
- [x] 1.2 在原光标位置增加有序内联 `@对象` token，并让标签进入 prompt 语义
- [x] 1.3 对齐新输入 `@` 触发、IME、整项删除、Escape、光标恢复、外部 prompt 回填和现有 `#` 菜单
- [x] 1.4 扩展逐图/普通任务栏草稿，保存轻量引用范围并保证草稿隔离

## 2. Canvas Picking And Request Context

- [x] 2.1 扩展 SelectionWatcher，在新输入 `@` 拾取期间优先捕获元素 ID，不触发图片替换绑定
- [x] 2.2 支持 Frame/分组子元素查找，并排除临时反馈节点和关系线
- [x] 2.3 增加类型化引用解析，复用图片、视频、文本和栅格化链路，补充音频选择字段
- [x] 2.4 在工作流、Agent、任务队列和重试上下文中透传有界引用快照
- [x] 2.5 对删除元素、换板、空画布、URL 错误及模型不支持的引用做阻止式错误反馈

## 3. Persistent Result Links

- [x] 3.1 发布成功并创建任务节点后立即固化来源并建立关系线，发布前失败时保留引用以便重试
- [x] 3.2 最终结果插入后把已有关系线从任务节点幂等迁移到首个稳定结果，不创建第二组线
- [x] 3.3 扩展关系线同步插件：任务节点仅可作为目标；移动端点时更新、删除任一端时只删除线
- [x] 3.4 覆盖批量任务、重复完成通知、跨画板完成与延迟补线、失败节点保留、任务节点删除和来源提前删除场景

## 4. Verification

- [x] 4.1 定向测试：开关、新输入 `@` 位置语义、内联 token 增删/范围平移、外部回填和草稿隔离
- [x] 4.2 定向测试：全元素解析、能力拒绝、工作流/任务透传和并发快照
- [x] 4.3 定向测试：发布任务即时连线、最终结果端点迁移、幂等、端点移动和删除清理
- [x] 4.4 运行 TypeScript、目标 ESLint、Prettier、OpenSpec strict 与 git diff 检查
- [x] 4.5 用真实浏览器验证桌面与 390px 移动视口的开关、`@` 拾取、中间插入、非选择工具恢复及现有功能回归；任务提交/连线沿用已通过的集成测试，未调用付费 Provider
- [x] 4.6 回归双图片输入法续写场景，验证稳定类型编号、两个来源独立建线及旧关系线黑色样式迁移
- [x] 4.7 回归慢任务期间续写、切板首帧样式迁移及并发长视频按工作流隔离迁线
- [x] 4.8 回归旧长视频缺少工作流 ID 的保守迁线，以及迁线异常后的缓存结果重试

## 5. Unrestricted Raster Sources

- [x] 5.1 更新设计与需求规范，明确来源元素数量和逻辑尺寸完全开放，资源边界由固定输出像素预算承担
- [x] 5.2 移除按来源后代元素数量、逻辑边长和逻辑面积拒绝栅格引用的路径
- [x] 5.3 保持引用串行解析，并按固定总像素与最长边预算对完整来源边界自适应降采样
- [x] 5.4 增加超多元素、超大逻辑边界、极端宽高比和多个大型引用的定向测试，验证完整解析、串行转换与输出像素预算
- [x] 5.5 运行目标测试、TypeScript、ESLint、Prettier、OpenSpec strict 与 git diff 检查
- [x] 5.6 在任务节点插入画布后立即按提交快照建立全部来源线，并保留提交返回后的幂等重试
- [x] 5.7 阻断拾取点击被普通选择链路二次消费，并在提交前从可信内联范围恢复全部引用
- [x] 5.8 取消视觉类画布联想引用的模型推荐数量前置限制，不截断图片、卡片、画框与图形
- [x] 5.9 将新旧联想关系线统一为无箭头平滑曲线，保持深色清晰样式和端点同步
